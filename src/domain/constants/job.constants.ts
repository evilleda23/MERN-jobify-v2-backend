export enum JOB_STATUS {
  PENDING = 'pending',
  INTERVIEW = ' interview',
  DECLINED = 'declined',
}

export enum JOB_TYPE {
  FULL_TIME = 'full-time',
  PART_TIME = 'part-time',
  REMOTE = 'remote',
  CONTRACT = 'contract',
  INTERNSHIP = 'internship',
}

export enum JOB_SORT_BY {
  NEWEST_FIRST = 'newest',
  OLDEST_FIRST = 'oldest',
  ASCENDING = 'asc',
  DESCENDING = 'desc',
}
